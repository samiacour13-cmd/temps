# لعبة مهمة إنقاذ الزمن - دليل الاستخدام

## 📁 ملفات اللعبة

### 1. time_machine_game.html
الملف الرئيسي للعبة، يحتوي على:
- واجهة المستخدم (HTML)
- التصميم (CSS)
- منطق اللعبة (JavaScript)

### 2. questions.json
ملف الأسئلة المنفصل، يحتوي على جميع التحديات مقسمة حسب المستويات:
- **easy**: المستوى السهل (5 أسئلة)
- **medium**: المستوى المتوسط (5 أسئلة)
- **hard**: المستوى الصعب (5 أسئلة)

---

## ✏️ كيفية تعديل الأسئلة

### فتح ملف questions.json وتعديل الأسئلة:

#### مثال على سؤال المستوى السهل:
```json
{
  "value": 3,
  "from": "ساعة",
  "to": "دقيقة",
  "answer": 180,
  "hint": "كل ساعة = 60 دقيقة"
}
```

#### مثال على سؤال المستوى الصعب (وضعية إدماجية):
```json
{
  "story": "فيلم مدته ساعتان و30 دقيقة. كم دقيقة هذا؟",
  "answer": 150,
  "hint": "ساعتان = 120 دقيقة، أضف 30"
}
```

### خصائص السؤال:

#### للمستوى السهل والمتوسط:
- **value**: القيمة المراد تحويلها (رقم)
- **from**: الوحدة الأصلية (نص)
- **to**: الوحدة المستهدفة (نص)
- **answer**: الإجابة الصحيحة (رقم)
- **hint**: التلميح المساعد (نص)
- **tolerance**: (اختياري) نسبة الخطأ المسموح بها للأرقام العشرية (افتراضياً 0.01)

#### للمستوى الصعب:
- **story**: نص الوضعية الإدماجية (نص)
- **answer**: الإجابة الصحيحة (رقم)
- **hint**: التلميح المساعد (نص)
- **tolerance**: (اختياري) نسبة الخطأ المسموح بها

---

## ➕ إضافة أسئلة جديدة

### خطوات إضافة سؤال جديد:

1. افتح ملف `questions.json`
2. اختر المستوى المناسب (easy, medium, hard)
3. أضف سؤالاً جديداً قبل القوس المربع المغلق `]`
4. لا تنسَ وضع فاصلة `,` بعد السؤال السابق

### مثال:
```json
{
  "easy": [
    {
      "value": 3,
      "from": "ساعة",
      "to": "دقيقة",
      "answer": 180,
      "hint": "كل ساعة = 60 دقيقة"
    },
    {
      "value": 4,
      "from": "ساعة",
      "to": "دقيقة",
      "answer": 240,
      "hint": "اضرب في 60"
    }
  ]
}
```

---

## 🚀 تشغيل اللعبة

### الطريقة الأولى: محلياً
1. ضع الملفين `time_machine_game.html` و `questions.json` في نفس المجلد
2. افتح ملف `time_machine_game.html` في المتصفح

### الطريقة الثانية: على خادم
1. ارفع الملفين على خادم ويب
2. تأكد من أن الملفين في نفس المجلد
3. افتح الرابط في المتصفح

---

## 🔧 حل المشاكل

### المشكلة: الأسئلة لا تظهر
**الحل**: 
- تأكد من وجود ملف `questions.json` في نفس مجلد ملف HTML
- تأكد من صحة تنسيق JSON (استخدم أداة التحقق من JSON)
- افتح Console في المتصفح (F12) لرؤية الأخطاء
- **ملاحظة**: إذا فشل تحميل ملف JSON، ستعمل اللعبة تلقائياً بالأسئلة الافتراضية المدمجة

### المشكلة: خطأ في تنسيق JSON
**الحل**:
- تأكد من وجود فواصل بين الأسئلة
- تأكد من إغلاق جميع الأقواس
- استخدم محرر نصوص يدعم JSON للتحقق من الصحة
- يمكن استخدام مواقع مثل [JSONLint](https://jsonlint.com/) للتحقق من صحة التنسيق

### المشكلة: اللعبة لا تعمل عند فتحها مباشرة
**الحل**:
- بعض المتصفحات تمنع تحميل الملفات المحلية لأسباب أمنية
- استخدم خادم محلي بسيط مثل:
  - **Python**: `python -m http.server 8000`
  - **PHP**: `php -S localhost:8000`
  - **Node.js**: استخدم `http-server` أو `live-server`
- أو ارفع الملفات على استضافة ويب

---

## ✨ الميزات الجديدة

### نظام تحميل ذكي:
- **مؤشر تحميل**: يظهر أثناء تحميل الأسئلة
- **نظام احتياطي**: إذا فشل تحميل ملف JSON، تُستخدم أسئلة افتراضية مدمجة
- **معالجة الأخطاء**: تسجيل تفصيلي للأخطاء في Console
- **التحقق التلقائي**: فحص توفر الأسئلة قبل بدء اللعب

---

## 📝 ملاحظات مهمة

1. **عدد الأسئلة**: كل مستوى يجب أن يحتوي على 5 أسئلة على الأقل
2. **الوحدات المدعومة**: يوم، ساعة، دقيقة، ثانية
3. **الأرقام العشرية**: استخدم نقطة `.` وليس فاصلة `,`
4. **اللغة**: استخدم اللغة العربية الفصحى المبسطة

---

## 📞 الدعم

في حالة وجود أي مشكلة أو استفسار:
- راجع دليل الاستخدام
- تحقق من تنسيق ملف JSON
- اتصل بالدعم الفني للديوان الوطني

---

**الديوان الوطني للتعليم والتكوين عن بُعد**
*المرحلة الانتقالية إلى السنة الأولى متوسط*